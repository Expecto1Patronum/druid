server:
  port: 19999
spring:
  application:
    name: druid-admin
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include='*':